import {DisjointSet} from "./disjoint_set.js";

function add_sorted(list, ) {
    
}

export function prims(adj_list) {
    let visited = new Array(adj_list.length).fill(false);
    let pq = [];
    while(pq.length != 0) {
        
    }
}